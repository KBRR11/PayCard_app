import 'package:flutter/material.dart';

class TarejetaPage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('TarejetaPage'),
      ),
    );
  }
}